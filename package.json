{
  "name": "inventrust-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:install": "npm install --prefix frontend",
    "frontend:build": "npm run build --prefix frontend"
  }
}
